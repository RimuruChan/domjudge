{% extends 'base.html.twig' %}

{% block menu %}
    {% include 'public/menu.html.twig' %}
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-12">
            <div class="error-template">
                <h1>
                    {{ status_code }} {{ status_text }}</h1>
                <div class="error-details">
                    抱歉，这里发生了一些错误：{{ status_text }}.
                </div>
                {% if exception.message is defined %}
                <div class="error-details m-4">
                    <code>{{ exception.message }}</code>
                </div>
                {% endif %}
                <div class="error-details">
                    请联系工作人员寻求帮助。
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
